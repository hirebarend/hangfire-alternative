{
  "name": "hangfire-alternative",
  "version": "0.1.8",
  "description": "",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "prettier --write --config prettier.config.js --list-different src/**/*.ts",
    "start": "node dist/main.js",
    "local": "npm run build && node dist/main.js",
    "test": "npm run build && mocha dist/**/*.test.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hirebarend/hangfire-alternative"
  },
  "author": "Barend Erasmus",
  "license": "MIT",
  "devDependencies": {
    "@types/uuid": "^9.0.3",
    "prettier": "^3.0.3",
    "tslint": "^6.1.3",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "mongodb": "^6.0.0",
    "uuid": "^9.0.0"
  }
}
